# Audit — PR #173 tip `6c79144`, and what was fixed

**Auditor:** Claude (this session)
**Target:** `fix/admin-authority-stabilize` @ `6c79144`
**Fix branch:** `fix/test-production-db-guard` (from `6c79144`) — commits `547f751`, `b49cd68`
**Worktrees:** `worktrees/audit-6c79144` (read-only), `worktrees/db-guard` (the fixes)
**Verdict at audit time:** NO PASS — three blockers. Two are now fixed; one remains.

> Nothing was pushed. `repository/cardetail1-stage2b` still holds uncommitted
> Waves work (`tests/admin-ops-wave1-authority.test.js`,
> `tests/portal-wave2-wave3-honesty.test.js`, `netlify/lib/receipt-projection.js`,
> `netlify/functions/admin-customer-requests.js`) which is **not** in `6c79144`
> and was left untouched.

---

## 1. FIXED — tests were writing to the production database

**Severity: P0.** `547f751`.

`repository/cardetail1-stage2b/.env` carries
`DIRECT_URL=…@db.prisma.io` and `DATABASE_URL=prisma+postgres://accelerate.prisma-data.net/…`
— production. 21 test files `require('dotenv/config')`; **16 of them write**:

```js
// tests/addon-financial-mutation.test.js:158
await prisma.paymentAttempt.deleteMany({ where: { bookingId: id } });
await prisma.quote.deleteMany({ where: { bookingId: id } });
```

There was no guard. `npm test` in that checkout mutated production money tables.

This also explains a result that had been misread as progress: runs inside
`stage2b` reported **0 cancelled / 16 failures** because they reached
production; a clean worktree with no `.env` reports **75 cancelled / 37
failures**, which is the honest result for a machine with no database.

**Fix:** `netlify/lib/db-safety.js` + one call in `tryGetPrisma()`. Engages only
when `NODE_TEST_CONTEXT` (set by `node --test`) or `NODE_ENV=test` is present,
so production runtime is unchanged — asserted by a test. Escape hatch:
`CD1_ALLOW_PRODUCTION_DB_IN_TESTS=i-understand-this-writes-to-production`
(a truthy-but-wrong value does not open it). The error names the variable and
host, never the connection string.

**Still to do (owner):** rotate the Prisma Accelerate key — it was exposed
earlier in this session by a faulty mask on my side.

---

## 2. FIXED — five tests pinned values that are not contracts

**Severity: P2.** `b49cd68`.

| Test | Pinned | Reality |
|---|---|---|
| `customer-portal-loading-ux` | `my-garage.js?v=20260805-sync-pr3` | tip bumped it to `?v=20260809-tech-tip` |
| `customer-vehicle-garage-removed` | same | same |
| `customer-vehicle-itemization` | same | same |
| `my-garage-portal` | `doesNotMatch(/customer-portal-pay/)` | matched the **comment** explaining the endpoint is retired |

The hosted-Checkout code is correct — the call is gone and
`startHostedCheckoutFallback` only toasts. Assertions now target the shape
(`?v=<anything>`, `post('customer-portal-pay'`) instead of the value.

---

## 3. OPEN — tip change can capture money without crediting it

**Severity: P0. Not fixed — needs a design decision.**

`netlify/lib/db/payment-authority-service.js` → `cancelActiveAttemptsForTipChange`

```js
try {
  await fetchImpl(`https://api.stripe.com/v1/payment_intents/${attempt.providerObjectId}/cancel`, …);
} catch (_) { /* best-effort cancel; attempt row still closed below */ }
await prisma.paymentAttempt.update({ where: { id: attempt.id }, data: { status: 'canceled' } });
```

Three problems compound:

1. **A second Stripe call now runs inside the advisory-lock interactive
   transaction** (`timeout: 14_000`), whose own comment says *"Stripe work
   inside the lock must stay short."* Cancel + create per request.
2. **The cancel is best-effort but the row is marked `canceled` regardless.**
   If Stripe never received the cancel, the PaymentIntent stays live while the
   local row says otherwise — an orphan.
3. **The webhook then refuses that money** (`payment-authority-service.js:743`):

```js
terminalWouldRegress = … || (attempt.status === 'canceled' && paymentIntent.status !== 'canceled')
```

**Failure path:** customer changes tip → cancel fails → customer pays the
Payment Element still mounted with the old client secret → Stripe captures →
webhook rejects → **money taken, invoice still shows due.**

### Options (not implemented)

- **(a)** Move the cancel outside the lock and make it idempotent/retryable;
  keep the row `canceled` only after Stripe confirms.
- **(b)** Let the webhook credit a PI whose attempt was cancelled *for tip
  change specifically* (needs a distinct status such as `superseded`, so a real
  cancellation still cannot settle).
- **(c)** Do not cancel at all; leave the old attempt open and rely on the
  amount mismatch to refuse it — the customer simply pays the current amount.

(b) is the smallest change that closes the money hole; (a) is the smallest that
reduces lock pressure. They are complementary.

---

## 4. Tip partition — verified sound

The design itself is right, and worth keeping.

| Property | Verdict | Evidence |
|---|---|---|
| Overpay | safe | settlement ledger writes `balanceCents`, not the PI total |
| Invoice inflation | safe | tip is `kind:'fee'`; `computeFinancialProjection` counts only `settlement`/`adjustment`/`refund` |
| `outstandingCreditCents` | safe | follows from the above |
| Duplicate tip credit | safe | unique `providerEventId = tip_${pi.id}` |
| Amount tampering | safe | `amount_received !== attempt.amountCents` guard preserved; plus `payment_tip_partition_mismatch` |
| Tip caps | safe | 40% of balance and $500, both applied whether the client sends `tipCents` or `tipPercent` |
| **Refund** | **gap** | `refundableCents = settledCents − refundedCents`, and `settledCents` excludes the tip — **a full refund cannot return the tip** |

The refund gap is a product decision, not a defect: many products do not refund
tips automatically. It is currently undocumented and unsurfaced in the UI.

---

## 5. Answers to the handoff's questions

1. **Tip partition safe?** Yes for overpay/credit/duplication. **No for refund** — see above.
2. **Idempotency / orphan PI?** `buildIdempotencyKey` includes `amountCents`, so a tip change mints a distinct key and no Stripe key collision occurs. The orphan risk is real and is item 3.
3. **Reuse of PaymentIntent — is "hide + Pay again" enough?** Not on its own; it depends on the cancel succeeding. See item 3.
4. **Receipt limited auth via sessionStorage?** Covers the lookup path. Not verified for magic-link/action-token deep links opened in a fresh browser with no `cd1_garage_*` keys — untested here.
5. **Create modal payload?** Not audited this pass.
6. **`Math.round(dollars * 100)`?** No exploitable edge found; the tip module clamps by two independent ceilings before the value is used.
7. **Cash/card on-site / reconcile / hosted-Checkout isolation regressions?** None found in the suite differential.
8. **Should tip reach the receipt and tech payout?** Not currently. Minimum contract without touching Stripe UI: expose `technicianTipCents` in the receipt projection as a separate line under the settled total, and leave `techPayoutAmount` alone until payout policy is decided.

---

## 6. Suite state

| Tree | tests | pass | fail | cancelled |
|---|---:|---:|---:|---:|
| `d4a830e` in `stage2b` (**reached production DB**) | 2337 | 2321 | 16 | 0 |
| `6c79144` clean worktree | 2292 | 2174 | 37 | 75 |
| `b49cd68` (this branch) | 2300 | 2186 | **33** | 75 |

The 75 cancelled are `cancelledByParent` on Postgres-dependent suites whose
`before` hook hard-fails without a database — deliberate (*"never soft-skip
Stage 1 financial invariants when DB is missing"*). They are not a code defect;
they are what a machine with no staging database looks like. **The fix is a
staging `DATABASE_URL`, which the Neon instance can now supply.**

---

## 7. Suggested order for whoever picks this up

1. Rotate the Prisma Accelerate key.
2. Decide item 3 — (b) is the smallest patch that closes the money hole.
3. Point a staging `DATABASE_URL` at Neon and re-run; the 75 cancellations should disappear and the remaining failures become meaningful.
4. Decide whether the tip appears on the receipt (§5.8).
5. Decide the refund-vs-tip policy (§4) and write it down, even if the answer is "tips are not refunded".

Untouched by design: `prototypes/` (still untracked — `publish = "."` would
serve it at `cardetail1.com/prototypes/`), Stripe Checkout / Payment Element,
home and `ny-metro-hub`.
